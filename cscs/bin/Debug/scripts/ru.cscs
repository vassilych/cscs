функция факториал (н) {
  если(!исИнтэгэр(н)) {
    экск="Factorial is for integers only (n="+н+")";
    ошибка(экск);
  }
  если(н<0) {
    экск="Negative number (n="+н+") for factorial";
    ошибка(экск);
  }
  если(н<=1) {
    вернуть 1;
  }
  вернуть н*факториал(н-1);
}
